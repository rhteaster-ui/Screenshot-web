import type {UserConfig} from 'vite';

// Legacy Vite configuration retained only for local tooling compatibility.
// The Vercel deployment uses the standard Next.js build and does not load this file.
const config: UserConfig = {};

export default config;
